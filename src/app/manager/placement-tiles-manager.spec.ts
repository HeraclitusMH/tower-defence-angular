import { PlacementTilesManager } from './placement-tiles-manager';

describe('PlacementTilesManager', () => {
  it('should create an instance', () => {
    expect(new PlacementTilesManager()).toBeTruthy();
  });
});
